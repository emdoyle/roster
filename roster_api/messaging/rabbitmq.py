# What interface does this need to expose to the rest of the server?
# publishing messages with arbitrary content to arbitrary topic (naming convention assumed)
# subscribing to messages with a callback on arbitrary topic (Actor inboxes)
